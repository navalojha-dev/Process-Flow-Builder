# Python dependencies for the Vercel deployment.
# The local CLI also installs these (via pyproject.toml). Keep this file
# minimal — Vercel's serverless function bundle limit is 50MB compressed.
python-pptx>=0.6.23
jsonschema>=4.21.0
httpx>=0.27.0
ulid-py>=1.1.0
python-dotenv>=1.0.0
streamlit>=1.32.0
